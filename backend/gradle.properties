# Gradle Configuration

# Stop the Gradle daemon after 1 hour of inactivity (default is 3 hours)
org.gradle.daemon.idletimeout=3600000

# Consider disabling file watching on macOS to prevent lock issues after sleep
# org.gradle.vfs.watch=false
